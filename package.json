{
  "name": "bloxorz",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host --port 4397",
    "build": "tsc && vite build",
    "preview": "vite preview --host --port 4397",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.7",
    "vitest": "^3.2.4"
  }
}
